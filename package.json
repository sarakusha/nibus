{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.11.1",
    "rimraf": "^2.6.3"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "test": "lerna run test",
    "clean": "rimraf packages/**/lib",
    "publish": "lerna publish --registry=http://192.168.20.111:4873"
  }
}
